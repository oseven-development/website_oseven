/** @format */

import React from 'react'
import {StyledConsultingHeadline} from './styled'

export const ConsultingHeadline = ({children, ...props}) => (
  <StyledConsultingHeadline {...props}>{children}</StyledConsultingHeadline>
)
